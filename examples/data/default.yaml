schemaVersion: "1.0"
id: default
description: Default parameter data for local v0 examples.
values:
  app:
    id: demo-app
  account:
    username: demo-user
    password: "${secret.demoPassword}"
  i18n:
    loginButtonText: Login
secrets:
  - account.password
